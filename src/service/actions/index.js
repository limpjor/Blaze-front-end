import { createAction } from 'redux-actions';


export const ORDER = createAction('ORDER', payload => payload);
export const PRODUCTS = createAction('PRODUCTS', payload => payload);
export const ALERT = createAction('ALERT', payload => payload);
export const EDITORDERS = createAction('EDITORDERS', payload => payload);
export const NEWORDER = createAction('NEWORDER', payload => payload);
